# Template for Streamlit Cloud's secrets panel.
#
# In your Streamlit Community Cloud app:
#   Settings → Secrets → paste the line below (without the "example" filename).
#
# The dashboard reads via a READ-ONLY role (`archery_duels_ro`) and connects
# over public TLS to the Hetzner box. Replace PASSWORD before pasting.

DATABASE_URL = "postgresql://archery_duels_ro:READONLY_PASSWORD@89.167.9.101:5432/archery_duels?sslmode=require"
